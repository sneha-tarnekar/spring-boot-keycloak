server:
  port: 9090

spring:
  h2:
    console:
      enabled: true

keycloak:
  realm: demo-keycloak
  auth-server-url: http://localhost:8081/auth
  resource: springboot-keycloak
  public-client: true
  bearer-only: true